@model ResiApp.Models.Torre
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Editar Torre";
}
<style>
    .torre-form-container {
        max-width: 570px;
        margin: 2.7rem auto 0 auto;
        background: #fff;
        border-radius: 0.75rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
        padding: 2.5rem 2.2rem 2rem 2.2rem;
    }

    .torre-form-title {
        color: #1a202c;
        font-size: 1.875rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        text-align: center;
        letter-spacing: -0.01em;
    }

    .torre-form-group {
        margin-bottom: 1.5rem;
    }

    .torre-form-label {
        font-weight: 600;
        color: #374151;
        margin-bottom: .5rem;
        display: block;
        font-size: 0.95rem;
        letter-spacing: .01em;
    }

    .torre-form-control {
        border-radius: 0.5rem !important;
        border: 1px solid #d1d5db !important;
        font-size: 0.95rem;
        padding: 0.75rem 1rem !important;
        background: #ffffff !important;
        color: #374151;
        width: 100%;
        transition: all 0.15s ease;
    }

        .torre-form-control:focus {
            border-color: #2563eb !important;
            background: #fff !important;
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
            outline: none;
        }

        .torre-form-control:hover {
            border-color: #9ca3af !important;
        }

    .torre-form-btns {
        display: flex;
        gap: 0.75rem;
        margin-top: 2rem;
        padding-top: 1.5rem;
        border-top: 1px solid #f3f4f6;
    }

    .btn-torre-guardar {
        background: #2563eb;
        color: #fff;
        font-weight: 600;
        padding: 0.7rem 1.75rem;
        border-radius: 0.5rem;
        border: none;
        font-size: 0.95rem;
        box-shadow: 0 1px 3px rgba(37, 99, 235, 0.2);
        transition: all 0.2s ease;
        cursor: pointer;
        flex: 1;
    }

        .btn-torre-guardar:hover {
            background: #1d4ed8;
            color: #fff;
            box-shadow: 0 2px 6px rgba(37, 99, 235, 0.3);
        }

        .btn-torre-guardar i {
            margin-right: 0.5rem;
        }

    .torre-back-link {
        color: #6b7280;
        font-weight: 500;
        text-decoration: none;
        padding: 0.7rem 1.75rem;
        border: 1px solid #d1d5db;
        border-radius: 0.5rem;
        background: #ffffff;
        transition: all 0.15s ease;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex: 1;
    }

        .torre-back-link:hover {
            background: #f9fafb;
            border-color: #9ca3af;
            color: #374151;
            text-decoration: none;
        }

        .torre-back-link i {
            margin-right: 0.5rem;
        }

    .text-danger {
        color: #dc2626;
        font-size: 0.875rem;
        margin-top: 0.25rem;
        display: block;
    }
</style>
<div class="torre-form-container">
    <div class="torre-form-title">Editar Torre</div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IdTorre)
        @Html.HiddenFor(model => model.FechaCreacion)
        @Html.HiddenFor(model => model.UsuarioCreacion)
        @Html.HiddenFor(model => model.FechaModificacion)
        @Html.HiddenFor(model => model.UsuarioModificacion)
        <div class="torre-form-group">
            <label class="torre-form-label" for="NombreTorre">Nombre o Distinción Torre</label>
            @Html.TextBoxFor(model => model.NombreTorre, new { @class = "torre-form-control", placeholder = "Ej: Torre 1, Edificio Central..." })
            @Html.ValidationMessageFor(model => model.NombreTorre, "", new { @class = "text-danger" })
        </div>
        <div class="torre-form-btns">
            <a href="@Url.Action("Index", "Torres")" class="torre-back-link">
                <i class="fas fa-arrow-left"></i> Cancelar
            </a>
            <button type="submit" class="btn-torre-guardar">
                <i class="fas fa-save"></i> Guardar Cambios
            </button>
        </div>
    }
</div>